services:
  server:
    image: server:latest
    build: ./app
    ports:
      - "5000:5000"
    environment:
      - APP_ENV=production
    volumes:
      - testvol:/app/output
  healthcheck:
    image: healthcheck:latest
    

volumes:
  testvol: